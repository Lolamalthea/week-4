<section id="curiosities">
    <div class="container">
      <h2 class="text-center">Mes <span class="underline">curiosités</span></h2>
      <div class="row">
        <% @curiosities.each do |curiosity| %>
          <div class="col-sm-3">
            <div class="item">
              <%= link_to(image_tag(curiosity.image_url, alt: curiosity.image_text, class: "picture"), curiosity.image_url) %>
            </div>
            <h3>
              <%= curiosity.name %>
            </h3>
            <p>
              <%= curiosity.description %>
            </p>
          </div>
        <% end %>
      </div>
    </div>
  </section>
